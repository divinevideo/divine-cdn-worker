{
  "name": "blossom-sdk-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production"
  },
  "dependencies": {
    "@noble/curves": "^2.0.1",
    "@noble/hashes": "^2.0.1",
    "blossom-server-sdk": "^0.8.0",
    "openpgp": "^6.2.2"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
