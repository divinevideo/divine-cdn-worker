---
title: "11:30:32 PM - November 16, 2025"
date: 2025-11-17T07:30:32.210Z
timestamp: 1763364632210
---

## Project Notes

Added duration limit enforcement to Blossom video upload system:

**Files Modified:**
1. `src/streaming/bunny-webhook.mjs` - Added duration check in `handleVideoEncoded()`, stores `duration-rejected:*` in KV
2. `src/index.mjs` - Added duration check in `handleGetBlob()` and .m3u8 endpoint handler
3. `src/streaming/playback-resolver.mjs` - Added duration check in `resolveUrl()` to block all playback

**Key Details:**
- User-facing limit: 6 seconds
- Actual enforcement: 7 seconds (tolerance)
- Rejection stored in KV: `duration-rejected:{sha256}`
- Error response: HTTP 400 with helpful message
- Videos kept in BunnyStream, just blocked from serving

**Testing Notes:**
- Need to test with video > 7 seconds to verify webhook rejection
- Check all serving paths: direct GET, HLS .m3u8, PlaybackResolver
- Verify error message is helpful for users

